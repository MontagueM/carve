{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "scripts": {
    "bc:start:node": "npx hardhat node",
    "bc:deploy:contract": "npx hardhat ignition deploy ignition/modules/carve.ts --network localhost",
    "bc:compile": "npx hardhat compile && cp artifacts/contracts/carve.sol/carve.json frontend/src/contracts/"
  }
}
